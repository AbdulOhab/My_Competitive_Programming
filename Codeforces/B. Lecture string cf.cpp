#import<bits/stdc++.h>
#define t string
using namespace std;

int main()
{
    int n,m;
    map<t,t>f;
    t s,S;
    for(cin>>n>>m; m--; f[s]=S)cin>>s>>S;

    for(; n--; cout<<(s.size()>f[s].size()?f[s]:s)<<' ')cin>>s;
}
