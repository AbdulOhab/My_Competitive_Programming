#include <cstdio>
#include<iostream>
using namespace std;

int main ()
{
 int num,
  while (num > 0)
    {
      rem = num % 2;
      binary[index++] = rem;	

      if (rem == 1)
	parity++;

      num = num / 2;
    }

}

printf ("Hello World");

return 0;
}
