#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
using ull = unsigned long long;

int main(int ab_ohab, char const *abohab[]){
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
  int n;
  cin>>n;
  int arr[n];
  for(int i=0;i<n;i++)
  {
    cin>>arr[i];
  }
  // sort(arr,arr+n);
  // for(auto u:arr) cout<<u<<" ";
  int mn=0,mx=0,int t=3;
  int arr2[123];
  for(in i=0;i<n;i++)
  {
    if(t!==3)
    {
      nm=min(min,arr[i]);
      t++;
    }
  }



	return 0;
}
