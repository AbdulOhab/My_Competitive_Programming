#include<algorithm>
#include <iostream>
#include <cstdio>
#include <cmath>
#include <cstring>
#include <vector>
using namespace std;
using ull=unsigned long long;
typedef long long ll;

int main()
{
  ll a,b,c,d;
  while((cin>>a>>b))
  //while( scanf("%d %d",&a,&b!=EOF )
  {
    //std::cin>>a>>b;
    std::cout<<abs(a+b)<<endl;
  }

  return 0;
}
